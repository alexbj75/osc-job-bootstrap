# Intentionally empty: the OSC job runner pip-installs this file before the
# worker command runs, and bootstrap.py uses only the Python standard library.
# Every dependency added here becomes pre-execution attack surface.
